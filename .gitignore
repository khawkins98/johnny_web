# game data files (user-supplied, proprietary — do not commit)
/public/data

# build output
dist
traces/
dumps

# Vite dev-server dependency pre-bundle cache
.vite/

# tooling
node_modules
coverage/
.DS_Store
server-log.txt

# session scratch, agent worktrees, and ad-hoc probe output (never commit --
# these hold working notes, findings, and out-of-repo tooling like the patched
# dosbox-x tree; committing them is never intended)
scratchpad/
track4-out/
.claude/worktrees/
