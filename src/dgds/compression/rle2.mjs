// Kept for future adaptation: RLE2 decompression — no known game files use this
// format currently, but the slot is reserved in the CompressionType dispatch table.
export const decompressRLE2 = () => {
    throw 'Decompress Type RLE not implemented';
};
